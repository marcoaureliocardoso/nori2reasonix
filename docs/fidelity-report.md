# Conversion fidelity snapshot — senior-infra-ops-analyst

Generated by running the converter against the real skillset:

```
node bin/nori2reasonix \
  --input /mnt/c/projects/senior-infra-ops-analyst/senior-infra-ops-analyst \
  --output /tmp/n2r-real-workspace --target workspace
```

Result (inventory printed by the converter at the end of the run):

```
skills:     25
subagents:  12 (runAs: subagent)
commands:   20
hooks:      4 events (PreToolUse, PostToolUse, PreCompact, PostCompact)
warnings:   40 (see below — none are silent drops)
nori2reasonix: wrote 141 file(s)
```

## Verified in the emitted workspace

- 37 directories under `.reasonix/skills/` (25 skills + 12 subagent profiles).
- 20 commands under `.reasonix/commands/`.
- `.reasonix/settings.json` has all four hook events; `PreToolUse[0]` is
  `{ "match": "bash|Bash", "command": ".reasonix/skills/command-driven-operations/scripts/command-guard-launcher.sh pre", "timeout": 7000 }`.
- `REASONIX.md` emitted from the skillset's `AGENTS.md`.
- Sidecars copied: `command-driven-operations/{scripts,templates,references}`,
  context-continuity scripts, and every skill's `templates/`.

## Degradations (all surfaced as warnings, none silent)

| Field | Behavior |
|---|---|
| `tools: WebSearch` | unmapped warning (not remapped to `web_fetch`) |
| `tools: Skill, Task` | unmapped warnings |
| `disallowedTools` | per-tool warnings (Reasonix has no negative allowlist) |
| `model: inherit` | silently omitted (per design default) |
| `skills:` preload | carried into the subagent body as `run_skill` directives |
| `hooks` ask-semantics | documented fail-closed `ask→deny` (see `docs/hook-semantics.md`) |
